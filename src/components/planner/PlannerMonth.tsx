/**
 * Planner Month View
 * 
 * Wrapper for monthly planner with life area filtering
 */

import { PlannerMonthly } from './PlannerMonthly';

export function PlannerMonth() {
  // For now, just render the existing monthly view
  // Future: Add life area filtering via URL params
  return <PlannerMonthly />;
}
