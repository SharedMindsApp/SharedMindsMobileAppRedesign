/**
 * Planner Week View
 * 
 * Wrapper for weekly planner with life area filtering
 */

import { PlannerWeekly } from './PlannerWeekly';

export function PlannerWeek() {
  // For now, just render the existing weekly view
  // Future: Add life area filtering via URL params
  return <PlannerWeekly />;
}
