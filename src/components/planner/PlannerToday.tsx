/**
 * Planner Today View
 * 
 * Daily planner with intentions and reflections
 * 
 * Note: PlannerDailyV2 already includes PlannerShell.
 * Panels (IntentionsPanel, ReflectionsPanel) will be added
 * when PlannerDailyV2 is refactored to accept children/slots.
 * For now, rendering PlannerDailyV2 directly.
 */

import { PlannerDailyV2 } from './PlannerDailyV2';

export function PlannerToday() {
  // TODO: Add IntentionsPanel and ReflectionsPanel
  // This requires refactoring PlannerDailyV2 to accept children/slots
  // or extracting its content logic to a separate component
  return <PlannerDailyV2 />;
}
